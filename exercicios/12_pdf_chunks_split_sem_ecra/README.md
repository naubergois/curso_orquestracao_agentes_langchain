# Exercício 12 — PDFs, extração de texto e *chunking* (sem ecrã)

Notebook Jupyter: gerar PDFs pedagógicos com **ReportLab**, ler com **pypdf**, comparar **`RecursiveCharacterTextSplitter`** vs **`CharacterTextSplitter`**, variar **overlap** e associar **metadata** (`Document`).

Não é obrigatório **API Gemini** neste exercício (só bibliotecas locais).

## Arranque

```bash
./run.sh
```

Abrir `exercicio_12_sem_ecra.ipynb`.
