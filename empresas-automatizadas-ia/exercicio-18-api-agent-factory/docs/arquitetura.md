# Arquitetura — Exercício 18: API Agent Factory

## Visão técnica
Três operações HTTP (`/chat`, `/classificar`, `/resumir`) podem ser testadas com `httpx` contra API em execução **ou** com funções puras no notebook espelhando as chains.
