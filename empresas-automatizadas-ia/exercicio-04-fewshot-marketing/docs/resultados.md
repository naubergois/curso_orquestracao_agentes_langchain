# Resultados esperados — Exercício 04

- API `GET /health` com `status: ok`.
- `POST /campanhas/gerar` devolve JSON compatível com `Campanha` (campos preenchidos, ≥3 hashtags com `#`).
- Notebook executa e imprime uma campanha para o briefing exemplo (produto agentes / público tech).
- Alterar `estilo` para `provocativo` ou `formal` muda perceptivelmente o tom mantendo o schema.
