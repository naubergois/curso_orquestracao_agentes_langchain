# Docker Compose lê o `.env` na RAIZ do repositório do curso (../../.env).
# Este ficheiro documenta chaves comuns; opcional para sobrescrever no modo local.

GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Opcional: modelo só para este exercício (senão usa GEMINI_MODEL).
# GEMINI_MODEL_EX04=gemini-2.0-flash
PORT=8000
