# Docker Compose: variáveis no `.env` na RAIZ do repositório do curso (../../.env).
# Opcional: copie este ficheiro para `.env` nesta pasta para sobrescrever só no modo local.

GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
