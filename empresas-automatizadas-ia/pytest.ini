[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra -q
markers =
    integration: chamadas reais a APIs externas (requer chaves no .env)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
